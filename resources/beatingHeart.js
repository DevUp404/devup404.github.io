// Heart beating on page site

function insert(){let foo = document.createElement('div');foo.setAttribute("class", "heart");let bar = document.createElement('div');bar.setAttribute("class", "heartBox");let abc = document.createElement("style");abc.textContent = `.heartBox {position: fixed;bottom: 0; top: 50;right: 0;left: 0;font-size: 10rem; animation: heartBox .5s infinite alternate linear;} @keyframes heartBox { from { transform: scale(.9);} to { transform: scale(1);}} .heart{width: 100px;height: 100px;transform: rotate(45deg);transform-origin: center;position: relative;background: red;margin: 100px auto;} .heart::before, .heart::after{content: '';position: absolute;width: 100px;height: 100px;border-radius: 50%;background: red;} .heart::before{left: -50px;
} .heart::after{top: -50px;}`;bar.appendChild(abc);bar.appendChild(foo);document.body.appendChild(bar);} insert()
function insert(){let foo = document.createElement('img');foo.setAttribute("src", "https://pngtree.com/free-hearts-vectors");foo.setAttribute("width", "100px");let bar = document.createElement('div');bar.setAttribute("class", "heart");bar.appendChild(foo);let abc = document.createElement("style");abc.textContent = `.heart {position: fixed;bottom: 0;right: 0;font-size: 10rem; animation: heart .5s infinite alternate linear;} @keyframes heart { from { transform: scale(.9);} to { transform: scale(1);}}`;bar.appendChild(abc);document.body.appendChild(bar);} insert()
